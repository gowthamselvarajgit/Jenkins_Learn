pipeline {
    agent {
        docker {
            image 'alphine'
        }
    }
    stages{
        stage('Run Command in Docker'){
            steps{
                echo 'Starting Docker container...'
                sh 'echo "Running in Docker Container"'
            }
        }
    }
}